import { CollaboratorSectionPipe } from './collaborator-section.pipe';

describe('CollaboratorSectionPipe', () => {
  it('create an instance', () => {
    const pipe = new CollaboratorSectionPipe();
    expect(pipe).toBeTruthy();
  });
});
